# 28 — Release on the TEST site, September 12, 2026: rules, attendance, waiver records, design, isolation

**TEST only.** Nothing here has been deployed to the league (production) site or run on the production database. The
production steps in section 9 are prepared for review and wait for the organizer's approval.

- TEST site: https://chriz93.github.io/dcbadmintonclub-test/ (repository `dcbadmintonclub-test`, branch
  `upgrade/secure-platform`, Supabase project `wgolevihkvmosajumzvl`).
- League site: https://chriz93.github.io/dcbadmintonclub/ (repository `dcbadmintonclub`, Supabase project
  `bwepvxelvwgwxrnaglrx`). **Unchanged.**

## 1. What changed

| Area | Change | Where |
|---|---|---|
| Late rule | "Players arriving more than 5 minutes late move down one court." A late player moves to the next court in use below; a player already on the bottom court in use stays. The player also stays when the court below already has five players, when either court already has scores this round, or when moving would leave one player alone. Court sizes always stay valid. | Home rules, registration rules, Session → Late Arrivals, Attendance → Late (p36, p37) |
| Shuttlecocks | Two per player on each court: 8 for four, 10 for five, 6 for three, 4 for two. Players provide extras. The Session tab's hand-out form shows each court's own number and refuses a count above it. | p36 |
| Removed | "Repeated unexcused absences may result in losing your spot to a waitlist player." | p36 |
| Added | "The app can make mistakes. Please keep a note of your court's score for each round on your phone. If you notice an error, send your recorded scores to the organizer on the same day, after the session." It appears on the Home rules, in the registration rules, on the registration confirmation and on the score page. | p36 |
| Payment acknowledgement | Only on registration step 1, where the fee is chosen; the duplicate boxes on the waiver step are gone. | p36 |
| Fees | Shown only where they apply: a spare sees $20, a regular $400, and the organizer and people with no player record see both. On registration, only the chosen membership's fee is shown. | p36 |
| Attendance | ✅ Present / ⏰ Late / ❌ Absent toggles for every seated player, at least 44px tall on phones, plus "Here after all" for anyone taken off. A toggle only records attendance. | p37 |
| Adjust courts | One button for the round about to be played. It previews every change in plain words, allows changing any move (checked again before it can be applied) and marking courts unavailable, then applies in one undoable save. The last adjustment stays on the tab with an Undo button. | p37, p37b, p40, p41, p41b |
| Two players | Best of three: after a 2–0 there is no Game 3 (not shown, not saved, not needed to finish the round). The database refuses a Game 3 after a 2–0 (migration L19). | p37, L19 |
| Ladder gaps | "One court up/down" means the next court in use, so a court whose players are all out never leaves anyone alone after a rotation. | p37 |
| Late player who stays | When a late player has to stay (the court below is full or has scores, or leaving would leave someone alone), Apply records the stay and says "Recorded for Round N — no court changes". Previously the late mark stayed waiting, so a later adjustment could still move them. The preview's Apply button and Apply itself now use one rule: Apply is available whenever it would save something (a court change, a late arrival to settle, or a change to the unavailable courts). | p46, p49 |
| Session start | Refused with the reason when nobody, exactly one player, or more than 30 players (six courts × 5) are coming. | p37b, p37c, p45 |
| Waiver | Versioned wording kept in the database, an acceptance record for every agreement, a prompt to accept an updated version, and admin-only downloads. v2 (the revised draft) is current on TEST; v1 stays current on the league site until a lawyer reviews v2. | L20, T02, p38, p41; review in docs/30 |
| Design | Court-green palette with every text colour at least 4.5:1, condensed display headings with a plain body face, a 12px minimum text size, 44px touch targets on phones, a visible focus ring, short fades, and no motion when reduced motion is requested. | p39 |
| Accessibility | Fixes for problems the new automated checks found:<br>• every select has a name;<br>• inactive rows are no longer faded into unreadable text;<br>• avatar and share-button colours are readable;<br>• dialogs are announced by their title, take the focus, keep Tab inside, close with Escape and return the focus, even when the button that opened them was disabled or redrawn meanwhile;<br>• the court cards open from the keyboard;<br>• the note buttons say whose note they are;<br>• on touch screens every checkbox and radio row is 44px tall;<br>• long messages wrap inside a phone screen;<br>• the round progress bars show the session's two rounds (they showed 99 dots). | p43–p51 |
| Background refresh | A refresh that left the database just before a save and arrived after it no longer undoes that save on screen. Before, the saved scores could vanish until the next refresh, the next save could be refused as out of date, and a save that completed a round could miss the automatic advance. | p52 |
| TEST outage fix | After this release reached TEST, everyone who signed in saw "Cannot connect to database". Two waiver queries relied on an implicit order by `created_at`, a column the new `waiver_versions` table does not have, and the real database refused them. Both queries now name their order. The stand-in the tests use now refuses unknown columns on the new tables as the database does, and every suite fails on such a refusal. | p53 |
| Isolation | The site checks the database it is connected to (address before loading; database marker after sign-in). Tests, seed scripts and builds refuse production (section 6). The new tables grant reading only, with no TRUNCATE for any site role. | p35, p42, L18, L21 |

## 2. How the attendance adjustment decides (the rules the tests check)

These rules apply only to a running session, and only to courts with no score yet in the current round. A court with
scores keeps its players until the round ends.

1. **Absent players** leave their court.
2. **A player marked present after being taken off** goes back to the court they left. If that court is full, has scores
   or is unavailable, they are placed like the players in step 3.
3. **Players who must move** are placed one at a time: players on an unavailable court, the most recent arrivals on a
   court over five, and returning players who can't go back. Each goes to, in order of preference:
   - the nearest court in use **below** with room;
   - otherwise, a free court below, if another of these players can join them there (so a group from an unavailable court
     keeps playing together);
   - otherwise, the nearest court in use above with room;
   - last, any free court.
4. **Late players** move down one court, as in the late rule above.
5. **Nobody plays alone.** A lone player joins the next court in use below; on the bottom court in use, they join the
   court above as its fifth player (the existing rule).
6. **Nothing else moves.** Nobody is promoted because someone else is missing, and short courts are not merged: a court
   of two plays best of three, three play three singles games, four play three doubles games to 21, and five play five
   doubles games to 15, each sitting out once.
7. **Refused, and nothing changes:**
   - exactly one player left;
   - more players than the available courts hold (five each);
   - a lone player with nowhere to go;
   - closing a court that already has scores.

   The reason is shown.
8. **Initial allocation stays separate.** Start Session still seats the night by ranking: players who are coming, by
   earned court, four per court, with extras as fifth players on Courts 6, 5, 4 and so on. Adjust courts never re-ranks.

**Assumption to confirm.** Votes known before the session (players who declined in time) are part of that initial,
ranked seating. Absences discovered at the gym use Adjust courts, which preserves assignments. If the organizer wants
declines handled the preserving way too, that is a small change to Start Session.

## 3. How to use it (organizer)

- **Mark absences on a phone.**
  1. Admin → 📋 Attendance.
  2. Tap ✅ Present, ⏰ Late or ❌ Absent next to each player; each tap saves at once.
  3. The bar at the top says how many court changes are waiting.
- **Adjust courts.**
  1. Press **🔁 Adjust courts**. The preview lists every change, for example "Asha is absent — off Court 2." or "Dev was
     the only player left on Court 4, so joins Court 5".
  2. If you want, change a move's court, or open "Courts unavailable tonight".
  3. Press **✓ Apply changes**, or **Cancel** (Escape also closes the preview).
  4. If the only news is a late player who has to stay, Apply records that they stay and says "Recorded for Round N
     — no court changes". They won't be moved later.
- **Undo.** After applying, the "Last court adjustment" card has **↶ Undo this adjustment**. The ↶ pill at the bottom and
  Tools → Undo work too. Each press goes back one step.
- **Late arrival.** Tap ⏰ Late on the Attendance tab (applied with the next Adjust courts), or Session → Late Arrivals →
  Mark Late (applied at once).
- **Waiver records.**
  1. Admin → 🖋 Waivers.
  2. **⬇ Record** next to a player downloads a readable file: every acceptance, each with the exact wording, version,
     fingerprint, UTC and Ottawa time, device time zone, how it was accepted, and the registration reference.
     **⬇ All acceptances (CSV)** downloads everything; **⬇ Wording** downloads a version's exact text.
  3. **Make current** changes the version new sign-ups see; registered players are then asked to accept it on Home.
  4. Players → 📄 Export now downloads the same record.

## 4. TEST admin access for a reviewer

- Send the reviewer the TEST site address.
- To give them organizer access, the organizer adds the reviewer's email to `app_admins` on the **TEST** project only.
  This is the P00 step with the TEST SQL editor, after the reviewer has signed in once.
- The reviewer then sets up their own authenticator app for the second factor.
- Never share the organizer's own sign-in, codes or keys.
- TEST email templates are marked "TEST". TEST players are synthetic `@example.invalid` addresses.
- **Players to try it with** (September 12): `T03_seed_synthetic_players.sql` added 22 synthetic regulars, placed 4/4/4/4/3/3
  on Courts 1–6, and 3 spares, with Session 1 votes (20 regulars coming, 2 declining, 2 spares asking for a seat).
  - Every address is `seed-player-NN@example.invalid`; no waiver acceptances were created.
  - It refuses any database not marked TEST (rehearsed locally, including the refusal) and adds nothing if run again.
  - To try it: sign in as the organizer, then Admin → Session → Start Session, then Attendance → Adjust courts.

## 5. Results

**Passing tests are not proof that there are no bugs** (section 7). They show that the behaviours they name work in
the situations they create.

| Suite | Command | Result |
|---|---|---|
| Browser, all projects | `cd platform && pnpm exec playwright test -c ../legacy/tests/e2e/playwright.config.ts` | 5,312 passed, 0 failed, 2 skipped (by design), in 17.9 minutes, on the final code (commit `0a2c2ac`) |
| Unit | `node --import ./legacy/tests/unit/fetch-guard.mjs --test legacy/tests/unit/adjust.test.mjs legacy/tests/unit/isolation.test.mjs legacy/tests/unit/helpers.test.mjs` | 518 of 518 passed |
| Automation (job code) | `cd legacy/automation && node --test` | 28 of 28 passed (12 new: backing up the waiver tables, the add-only restore, the test guards) |
| Database rehearsal | `sh legacy/tests/db/run.sh` | 17 rule phases passed; 1,850 of 1,850 cases passed; rollback rehearsal passed |
| verify.sql, fresh rehearsal | rules.sql, then fixtures, then verify.sql | 59 of 59 OK (`environment marker OK: test L21`, `one current waiver OK: 2026-09-v2`) |
| verify.sql, TEST project | Supabase SQL editor | 59 of 59 OK, run from the pushed commit `0a2c2ac` (`verify.sql` SHA-256 `ea244d2ab1a47f6e…`): `environment marker OK: test L21`, `one current waiver OK: 2026-09-v2` |
| Patch replay | apply p35–p52 to the committed `index.html` | identical to the tested file |
| Coverage inventory | `node legacy/tests/coverage-inventory.mjs` | no gaps (docs/29) |

**Counts.**

- Baseline: 3,965 browser cases, 1,602 database cases, 16 automation tests.
- New: 1,349 browser cases, 518 unit tests, 248 database cases and 12 automation tests — **2,127** in total.
- No baseline test was removed or renamed; this was checked by comparing test titles.
- Skipped: 2, the same two as the baseline. `season.spec` and `opener.spec` run once, on the desktop project only;
  their data is identical on every screen size.

**Tests changed because a requirement changed.** Each was updated to the new rule and still checks it exactly:

- Attendance toasts and the attendance lists.
- Late arrivals now go through the adjustment engine.
- Best of three.
- The removed absence sentence.
- Payment boxes only on step 1.
- Two per player shuttlecocks.
- Start Session with nobody coming is now refused (the unit scenario, p45).
- Apply is available when a late arrival must be recorded as staying or the unavailable courts change (adjust.suite,
  p46/p49).

**Defects found while testing this release, all fixed:**

| Found by | Defect | Fix |
|---|---|---|
| verify.sql on TEST | The service role could TRUNCATE, REFERENCE or TRIGGER on the new tables | L21 (and the rehearsal now copies Supabase's defaults) |
| New adjust tests | An override's format notes were stale; the waiver box re-rendered needlessly; a busy preview gave no answer; the Adjust button stayed disabled | p40, p41, p41b |
| Placement and seating tests | Valid rounds refused; one player seated alone at Start Session | p37b, p37c |
| axe checks | Selects without names, faded text, low-contrast avatars and share buttons | p43, p44 |
| Dialog and keyboard tests | The dialog was not announced and did not take the focus; Escape did nothing; the focus was lost after closing; the court cards could not be opened by keyboard; the note buttons had no useful name | p45, p46, p47, p48 |
| Evening sequences | A late player who had to stay was left waiting and could be moved later; the preview and Apply disagreed | p46, p49 |
| Phone waiver tests | The waiver choices were under 44px on phones | p50 |
| Exploratory review of the screenshots | Long messages ran off a phone screen; the round progress bars drew 99 dots | p51 (and three reflow tests) |
| The ten-session season simulation, failing at random sessions under load | A slow background refresh undid a save that had just been made: scores vanished from the screen, the next save was refused, and a round could miss its automatic advance. This probably predates this release. | p52. The new sync-race tests fail without it: 3 of 5 fail on the page without p52, and all 5 pass with it. |
| You, on the live TEST site after the deploy | "Cannot connect to database" for everyone signed in. Two waiver queries were ordered by a column the table does not have. Confirmed against the real TEST API (400 `42703` for the old query; the fixed query passes the column check). The stand-in had not checked column names, and my own checks of the live site were signed out. | p53, and the stand-in's column check |

Exploratory testing on the live TEST site is in section 6.

## 6. Production isolation and parity

**Same code, different settings.** TEST and the league site are built from the same `index.html`, `sw.js` and
`manifest.json`. `build-production.py` changes only the database address, the publishable key, the page title, the
cache name and the site path. The databases differ only in:

- the one-row environment marker (`test` or `production`);
- the current waiver version (TEST: v2 after T02; production would keep v1);
- the email templates (TEST's are marked "TEST");
- the data (TEST players are synthetic `@example.invalid` addresses).

**What keeps TEST work away from production:**

| Safeguard | What it does | Checked by |
|---|---|---|
| Site address check (p35, p42) | Before loading anything, the page compares its own address with the database it was built for. The test site refuses the production database; the league site refuses any database but the league one. No request is made. | isolation.spec.ts (served copies with swapped addresses); unit `isolation.test.mjs` |
| Database marker (L18, T01, P01) | Each database carries a marker that can't be renamed or removed. After sign-in, the test site stops unless it says `test`, before reading any league data. Anonymous visitors can't read it. | rules.sql PHASE14; isolation.spec.ts; TEST verify `environment marker (L18) OK: test L21` |
| Script guard (`assert_test_environment()`) | The TEST seed (L02), T02 and every script from `load-run-into-test.py` call it first. On a database not marked `test`, they stop before changing anything. | rules.sql PHASE14, DB cases |
| Local database tests (`run.sh`) | Refuse any setting that names Supabase, a pooler or either project. They create and drop databases only on the local rehearsal server. | run.sh; unit isolation tests |
| Browser tests (`isolation.ts`, harness) | A run refuses to start if any setting names production. Each request to production, another Supabase project, the dashboard or either live site is blocked and recorded. Every suite fails if anything was blocked. The app talks to an in-memory stand-in for the TEST project. | isolation.spec.ts; the check in every suite's `closeCtx` |
| Unit tests (`fetch-guard.mjs`) | Network access is refused. | unit suite |
| Production build (`build-production.py`) | Builds only from a committed, clean tree and prints the commit and file fingerprint, so production gets the tested file. | Not run in this release |
| Scheduled jobs | The automation patch is applied, at your request, in a safe form: under `node --test` both jobs refuse a real Supabase address. The daily backup includes `waiver_versions`, `waiver_acceptances` and `environment` when a database has them, and otherwise notes them as not there yet, so production's backup keeps running until L18/L20 reach it. Any other missing table or error still fails it. A restore adds missing waiver records only, never rewrites one, and leaves their guards on. | automation suite 28/28; Supabase's real reply for a missing table (404 `PGRST205`) was checked against TEST |

**What was run on the TEST database (project `wgolevihkvmosajumzvl`, September 12):**

1. A read-only pre-check: 0 real email addresses among past players, 29 past players, no new tables yet, 0 players.
2. `TEST_2026-09-12.sql` from commit c89c894 (SHA-256 `b33bff7e9c2257b2…`, loaded from GitHub and fingerprint-checked
   before running): L18, T01, L19, L20, T02.
   - verify: 57 of 58 OK.
   - The failure: `no TRUNCATE for site roles: FAIL: 3`. L18 and L20 had not revoked Supabase's default REFERENCES,
     TRIGGER and TRUNCATE grants from the service role on the three new tables.
   - The local rehearsal missed it because it did not copy those defaults. It now does, and PHASE17 reproduces the
     problem before checking the fix.
3. `L21_new_table_privileges.sql` (calls `assert_test_environment()` first on TEST). After it, the three tables grant only
   SELECT, to signed-in users and the service role; the marker reads `test L21`.

No script, test or check in this release wrote to the production database. The production checks were read-only:
the local production repository's commit and status, and the public production site.

**Production untouched (read-only checks, September 12):**

- The live league site's `index.html` and `sw.js` are byte-for-byte the production repository's commit `0a29460`
  (SHA-256 `04eda0ac0fec2c2d…` and `b6b80f0006a48f62…`).
- The site still points at the production database, with cache name `dcbc-v41`.
- The production repository has no changes except its untouched `.claude/` folder.

**Parity (after the TEST deploy):**

- `check-parity.sh` prints DIFFERENT, as expected until the production release. TEST now carries this release
  (fonts, palette and everything in section 1); the league site does not.
- After the push, the live TEST site serves exactly the committed `index.html`, `sw.js` and `manifest.json`, with cache
  `dcbc-test-v62`.
- The league site still matches production commit `0a29460` byte for byte.
- The first push (`0a2c2ac`) changed nothing under `.github/` or `legacy/automation/`. The later automation change keeps
  both production jobs working as before: the reminder job only gains a test-only guard, and the backup also saves the
  new tables once a database has them.

**Exploratory testing (beyond the scripted tests):**

- **Screenshots, reviewed by eye.** Twelve key screens on a laptop and on a phone (from the local TEST stand-in). This
  found two display faults no test had caught: long messages cut off on phones, and the round bars drawing 99 dots.
  Both are fixed in p51 and now tested.
- **The season simulation under load.** It failed at random sessions while other suites ran. Tracing it found a real
  race: a slow background refresh undid a save the page had just made (fixed in p52). A trace on the page without p52
  showed the saved scores disappearing (3 scores at version 2 dropped back to 0 at version 1). The new sync-race tests
  fail 3 of 5 without p52 and pass with it.
- **The live TEST site after the deploy** (commit `0a2c2ac`), signed out, on a laptop and at 375px:
  - the sign-in screen renders;
  - no console errors;
  - the only request is for the page itself, so nothing is read before sign-in and no production address is contacted;
  - the page identifies itself as the test site;
  - the service worker is active;
  - no sideways scrolling.
- **Signed in, on the live TEST site (after you reported the error).** I used the session already open in your browser;
  nothing was typed and nothing was changed.
  - Before the hotfix, it showed "Offline" and "Cannot connect to database", with
    `loadAll Error: Load waiver_versions failed (400)` in the console.
  - After the hotfix (`ef5639d`), it shows "Live", no error message and a successful page load. The current waiver v2
    and its wording (7,540 characters) load, and the marker reads `test L21`.
  - The TEST player list is empty, so the organizer flows that need players were exercised against the stand-in.
    Section 4 lists what is worth trying yourself.
- **Observations left as they are:**
  - The Adjust preview shows both Cancel and the dialog's own Close button. That is redundant but harmless.
  - A signed-out visitor sees both fees in the Home rules, as the fee rule intends: people with no player record see
    both.

## 7. Limitations and assumptions

- **Passing tests are not proof that there are no bugs.** They show that the behaviours they name work in the situations
  they create. Exploratory testing found defects that the scripted tests had not (section 5).
- **The browser tests use a stand-in, not Supabase.** They run the real page against an in-memory copy of the TEST API
  (`mock-supabase.ts`). The database rules are tested separately on a local Postgres rehearsal. That rehearsal copies
  Supabase's roles, its default table privileges (added in this release) and its sign-in functions, but not the rest of
  Supabase: sign-in emails, rate limits, the authenticator step, the API gateway or the Supabase dashboard.
- **Accessibility.** Automated checks (axe, WCAG 2.2 A/AA) cover every page, tab and main dialog for an organizer and a
  player, plus keyboard focus, reduced motion and 44px touch targets. Nobody has tested with a screen reader or a
  switch device. Contrast is checked only in the states the tests display.
- **Late arrivals.** The organizer decides who counts as more than 5 minutes late; the app applies the move.
- **Initial allocation** is unchanged (section 2, assumption to confirm).
- **Waiver.**
  - v2 is a draft for a lawyer to review. It is not legal advice, and nothing in it claims that players give up every
    claim (docs/30).
  - Records start with this release. Earlier registrations get no invented acceptance; the players are asked to accept
    the current version the next time they open Home.
  - Acceptance records can't be changed, so a restore from backup can only add them to an empty table.
- **Backups.** The daily backup includes the waiver tables and the marker on any database that has them. Production
  does not have them until the release, so there they are noted as not there yet. A restore of acceptance records only
  adds missing ones.
- **Fonts.** Barlow and Barlow Condensed load from Google Fonts. Offline, or if they are blocked, the page falls back to
  system fonts. Google receives the visitor's IP address when it serves the fonts.
- **Reference sites.** smashlytics.net and griffjonesgolf.com were used as inspiration only; no text, images or code
  was copied.
- **Signed-in checks on the live site.** My checks of the live TEST site were signed out, and the stand-in did not check
  column names, so a query the real database refused reached TEST (p53). The stand-in now checks columns on the new
  tables, and the older tables' queries run every day on the league site. After the hotfix I checked the live site
  signed in, using your open browser session. A signed-in look after each deploy is still worth doing.

## 8. Waiver and legal items

See docs/30. v2 must be reviewed by an Ontario lawyer before it becomes current on the league site. Organizer decisions
listed there:

- adults-only or not;
- the water-bottle rule;
- OCDSB permit terms;
- insurance;
- how long records are kept.

## 9. Production release plan (prepared, not executed)

Nothing in this section has been run. Every step needs the organizer's approval on the day, and steps marked **you**
need the organizer's own sign-in or keys. Allow about 45 minutes, on a day with no session and nobody registering.

**Decide before the day:**

- **Waiver version.** v1 stays the current waiver on production until a lawyer approves v2. Publishing v2 later is Admin →
  🖋 Waivers → Make current; it needs no migration.
- **Initial-allocation assumption** (section 2).
- **The one-time waiver card.** After the release, every registered player sees a card on Home asking them to accept the
  waiver, because nobody has an acceptance record yet and none are invented. Accepting records v1 with the time and
  device. Consider announcing it.

### A. Read-only checks (5 min)

1. TEST verify: every row OK (section 5).
2. `sh legacy/scripts/check-parity.sh` shows TEST ahead of production (expected until step D).
3. **you** Production SQL editor (the header must say `bwepvxelvwgwxrnaglrx`). This query only reads:

   ```sql
   select 'register_me (7 arguments)' as item, md5(prosrc) as value from pg_proc where oid=to_regprocedure('public.register_me(text,text,text,text,text,text,text)')
   union all select 'save_court_scores', md5(prosrc) from pg_proc where oid=to_regprocedure('public.save_court_scores(int,int,jsonb,int)')
   union all select 'environment table', coalesce(to_regclass('public.environment')::text,'absent');
   ```

   Expected: `04d96d172bfa33add059a202f0270d49`, `e2255fa62e57650d3609fc7547a95d7f`, `absent`. Any other result means production is not in the state these scripts expect;
   stop there.

### B. Backup (10 min)

This release changes and deletes no existing rows: L18, L20 and L21 add tables, and L19 and L20 replace two functions. The
backup is a precaution.

1. **you** Supabase → production → Database → Backups: note the time of the latest daily backup (Supabase's own copy).
2. **you** GitHub → Actions → "legacy-backup" → Run workflow. When it finishes, download its artifact to
   `~/MaplewoodBackups` and open it; the `players` count must match the site's player list. This is the copy held
   outside Supabase.
3. Optional: the in-database copy from docs/24 section B, with today's date in the schema name. It only speeds up a
   row-level undo; it is not an independent backup.

### C. Database (5 min) — do C and D back to back

At C, the previous site can no longer register anyone, because L20 replaces the function it calls. The new site needs
C, so publish D straight after.

1. **you** Production SQL editor: paste the whole of `legacy/migrations/PROD_2026-09-12.sql` and run it. It contains L18, P01,
   L19, L20, L21 and verify.
   - Before pasting, `shasum -a 256` of the file must start with `e0b644173c3fdcf5`.
   - Expected: every verify row OK, `environment marker (L18) OK: production L21`, and
     `one current waiver (L20) OK: 2026-09-v1`.
2. If a statement fails, the editor stops there. Don't publish D; keep the error. Each migration is its own transaction;
   the ones before the failure stay applied. Of those, only L20 affects the previous site.

### D. Site (5 min)

1. Build from the committed TEST release commit. The publishable key is reused from the current production build:

   ```bash
   cd /Users/christygeorge/dcbadmintonclub-test && python3 legacy/scripts/build-production.py --out ../dcbadmintonclub --reuse-key --cache dcbc-v42
   ```

   `--cache dcbc-v42` is required: production's cache is `dcbc-v41` and the script's default is older. The commit the
   script prints must be the TEST release commit.
2. Publish:

   ```bash
   cd /Users/christygeorge/dcbadmintonclub && git add index.html sw.js manifest.json && git commit -m "September 2026 release" && git push
   ```

3. `sh legacy/scripts/check-parity.sh` must print IDENTICAL.

### E. Scheduled jobs (5 min, after C)

Nothing to apply: the job change is already on the default branch. After C, the next daily backup includes
`waiver_versions`, `waiver_acceptances` and `environment` by itself; until then it notes them as not there yet.

**you** Run "legacy-backup" once and confirm its file includes `waiver_acceptances`.

### F. Smoke test (10 min)

1. **you** Open the league site, sign in and complete the authenticator step. No "wrong database" page appears.
2. Admin → 🖋 Waivers: v1 is current, v2 is listed, and there are no acceptances yet.
3. **you** Home: the one-time waiver card appears. Accept it; Admin → 🖋 Waivers → ⬇ Record shows the acceptance.
4. At the next session: Admin → 📋 Attendance shows the Adjust courts bar.
5. Registering a test player on production is a production write. Do it only if the organizer chooses to.

### Rollback

1. **Site.**

   ```bash
   cd /Users/christygeorge/dcbadmintonclub && git revert --no-edit HEAD && git push
   ```

2. **Database.** Production SQL editor: `legacy/migrations/ROLLBACK_2026-09-12.sql`. Generated by
   `build-rollback-sql.py`, which copies the text from L10 and L15.
   - It restores exactly the two functions the previous site uses, and keeps the new tables and every waiver record.
   - Before changing anything, it checks that the database has this release's versions, and refuses otherwise.
   - Expected rows: `OK`, `OK`, `OK`, then the number of records kept.
   - run.sh rehearses it on every database run: the previous site's registration works again, no record changes, and
     a second run refuses.
3. **Jobs.** Leave step E in place; the tables it backs up remain.
4. **Last resort, only if data must be discarded:** restore Supabase's daily backup. This replaces the whole database
   and loses everything since, so it needs the organizer's decision. The in-database copy is not a substitute for the
   backup.

### Do not

- Run `TEST_2026-09-12.sql`, T01, T02, L02 or any seed on production. They refuse to run there, but don't try.
- Make v2 current on production before the lawyer's review.
- Paste keys into chat or on a command line.
